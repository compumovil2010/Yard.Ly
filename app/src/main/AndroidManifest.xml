<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yardly">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ly"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ly"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".UsuarioEntrega"
            android:label="@string/title_activity_usuario_entrega"></activity>
        <activity android:name=".Search_results" />
        <activity android:name=".RestaurantProfile"></activity>
        <activity android:name=".CalificarOpinion" />
        <activity android:name=".Search_results" />
        <activity android:name=".CatalogoEmpresa" />
        <activity android:name=".VerInformacionEstablecimiento" />
        <activity android:name=".infoPedido" />
        <activity android:name=".editarProducto" />
        <activity android:name=".CarritoCompras" />
        <activity android:name=".Busqueda" />
        <activity android:name=".Calificar" />
        <activity android:name=".editarProductoRopa" />
        <activity android:name=".editarProductoPlato" />
        <activity android:name=".AsignacionDomi" />
        <activity android:name=".ActividadBaseE" />
        <activity android:name=".omgreen" />
        <activity android:name=".entregaUsuario" />
        <activity android:name=".dirFrec" />
        <activity android:name=".editarPerfilU" />
        <activity android:name=".Producto" />
        <activity android:name=".PedidoHistory" />
        <activity android:name=".RegistroFoto" />
        <activity android:name=".Registro" />
        <activity android:name=".Checkout" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".logActivity" />
        <activity android:name=".Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>